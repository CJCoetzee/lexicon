# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/

# Node
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Vector store data
backend/chroma_data/
chroma_data/

# Eval reports — only the dataset and runner are version-controlled.
# Reports are generated and committed selectively when we want to publish them.
# (Use `git add -f backend/eval/reports/<name>.json` to commit a specific one.)
backend/eval/reports/*.json

# Uploaded documents (during dev)
backend/uploads/
uploads/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# OS
Thumbs.db
desktop.ini
