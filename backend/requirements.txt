# Pinned to the manylinux/MSVC wheel on PyPI to avoid the MINGW-W64
# Windows wheel that some mirrors ship and that prints scary warnings.
numpy==1.26.4

flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
google-genai==0.3.0
chromadb==0.5.5
pypdf==4.3.1
gunicorn==22.0.0
pytest==8.3.2
pytest-cov==5.0.0
ruff==0.5.7
